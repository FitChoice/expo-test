описание проекта

Архитектура: Feature-Sliced Design (FSD)  
Технологии: React Native, Expo, MediaPipe, TanStack Query, Zustand


Структура

src/
├── app/              / Слой приложения (роуты, провайдеры)
├── screens/          / Экраны приложения
├── widgets/          / Сложные UI блоки
├── entities/         / Бизнес-сущности
└── shared/           / Общие ресурсы



Слой приложения

 `app/_layout.tsx` - корневой layout приложения  
Зачем нужен
- Обертка всех роутов в AppProvider
- Подключение глобальных стилей (global.css)
- Базовая структура навигации

Что использует
- `AppProvider` - главный провайдер приложения
- `Slot` из expo-router - точка монтирования роутов / надо разобраться с экспо что такое и зачем нужен был, нужен ли сейчас



 `app/index.tsx` - Главная страница приложения  
внутри 
- Точка входа в приложение
- Рендерит HomeScreen

внутри
- `HomeScreen` из `@/screens/home`


`app/providers/AppProvider.tsx` - Главный провайдер приложения  
нужен чтобы 
- Объединять все провайдеры в один
- Оборачивать приложение в SafeAreaProvider
- Подключать TanStack Query через QueryProvider
внутри 
- `SafeAreaProvider` - для безопасных зон (notch, bottom bar)
- `QueryProvider` - для работы с API и server state



`app/providers/QueryProvider.tsx` - Провайдер для TanStack Query  
зачем 
- Настройка клиента для работы с API
- Кэширование запросов
- Управление состоянием загрузки данных


экраны

screens/home/ui/HomeScreen.tsx` главный экран приложения  

┌─────────────────────┐
│      Header         │ ← Навигация
├─────────────────────┤
│                     │
│    PoseCamera       │ ← Камера с детекцией поз
│    (fullscreen)     │
│                     │
├─────────────────────┤
│      Footer         │ ← Copyright
└─────────────────────┘

- `Header` из `@/widgets/header`
- `Footer` из `@/widgets/footer`
- `PoseCamera` из `@/widgets/pose-camera`


 `screens/pose/ui/PoseScreen.tsx` Fullscreen экран детекции поз  

- Отображение только камеры (без Header/Footer)
- Использование для отдельного роута `/pose`


┌─────────────────────┐
│                     │
│                     │
│    PoseCamera       │ ← Fullscreen камера
│    (fullscreen)     │
│                     │
│                     │
└─────────────────────┘

- `PoseCamera` из `@/widgets/pose-camera`

Список виджетов

`widgets/header/ui/Header.tsx` менюшка верхняя  

- Отображение логотипа "ACME"
- Навигационные ссылки (About, Product, Pricing)
- Адаптация под safe area (notch)
содержит
- Логотип (ACME) - ссылка на главную
- 3 навигационных ссылки (пока все ведут на /)

- `expo-router` для навигации /может стоит заменить? 
- NativeWind для стилей
- `useSafeAreaInsets` для отступов

`widgets/footer/ui/Footer.tsx` нижний колонтитул  

- Отображение copyright
- Адаптация под safe area (bottom bar)

- Скрыт на нативных платформах (native:hidden)

- NativeWind для стилей
- `useSafeAreaInsets` для отступов

`widgets/pose-camera/ui/PoseCamera.tsx` камера с детекцией поз через MediaPipe  

- Захват видео с камеры
- Детекция поз в реальном времени
- Обработка landmarks (точек тела)
- Логирование координат точек (только в dev mode)

- `@thinksys/react-native-mediapipe` - детекция поз
- Fullscreen режим (занимает весь экран)

как действует? 
1. Открывает камеру
2. Получает landmarks от MediaPipe (33 точки тела)
3. Преобразует в типизированный `PoseLandmark[]`
4. Логирует координаты через `logLandmarks()` из entities/pose

- `face={false}` - отключена детекция лица
- Размеры: полный экран устройства


- `PoseLandmark` тип из `@/entities/pose`
- `logLandmarks()` функция из `@/entities/pose`


 `entities/pose/model/types.ts` типизация данных о позах  

- Описание структуры данных MediaPipe
- Индексы точек тела (33 точки)

`PoseLandmark`
Одна точка на теле человека
```typescript
{
  x: number        // 0-1 (нормализованная позиция по горизонтали)
  y: number        // 0-1 (нормализованная позиция по вертикали)
  z: number        // Глубина (расстояние от камеры)
  visibility: number // 0-1 (уверенность детекции)
}
```

 `PoseData` Полные данные о позе
```typescript
{
  landmarks: PoseLandmark[]  // Массив всех 33 точек
  timestamp: number          // Время захвата
}
```

`PoseLandmarkIndex`
Перечисление всех точек тела (enum):
- Лицо: NOSE, LEFT_EYE, RIGHT_EYE и т.д. (0-10)
- Верх тела: SHOULDER, ELBOW, WRIST, пальцы (11-22)
- Низ тела: HIP, KNEE, ANKLE, пятка, стопа (23-32)

#### `PoseAnalysis`
Результат анализа позы
```typescript
{
  isValid: boolean      // Поза валидна?
  confidence: number    // Уверенность (0-1)
  message?: string      // Сообщение об ошибке
}
```

`entities/pose/lib/analyzer.ts` Функции для анализа поз  

- Валидация точек и поз
- Расчет расстояний и углов
- Логирование (только dev mode)

функции
`isLandmarkValid(landmark): boolean`
Проверяет видна ли точка достаточно хорошо
- Использует `MIN_VISIBILITY` из config (0.5)
- Возвращает `true` если точка уверенно детектирована

 `calculateDistance(point1, point2): number`
Вычисляет Евклидово расстояние между двумя точками
- Учитывает x, y, z координаты
- Полезно для измерения длины конечностей

 `calculateAngle(point1, point2, point3): number`
Вычисляет угол между тремя точками (в градусах)
- `point2` - вершина угла (например, локоть)
- Возвращает угол 0-180°
- Полезно для проверки правильности упражнений

 `validatePose(poseData): PoseAnalysis`
Валидирует всю позу целиком
- Проверяет количество видимых точек
- Вычисляет общую уверенность
- Сравнивает с `MIN_CONFIDENCE` (0.5)

 `logLandmarks(landmarks): void`
Логирует координаты всех точек в консоль
- Работает только в `__DEV__` режиме
- Форматирует числа до 3 знаков после запятой
- Используется для отладки


- `@/shared/config` - константы детекции
- Типы из `../model/types`

 `entities/pose/index.ts` публичный API entity  

- Типы: `PoseLandmark`, `PoseData`, `PoseAnalysis`
- Enum: `PoseLandmarkIndex`
- Функции: все из analyzer.ts

общие переиспользуемые элементы: 

`shared/ui/Button/Button.tsx`

- Универсальная кнопка для приложения

Props:
- `title: string` - текст кнопки
- Все props от `TouchableOpacity`

Стили:
- Indigo фон с закругленными краями
- Белый текст, жирный шрифт
- Тень для глубины

`shared/ui/Container/Container.tsx` - контейнер с отступами  

- Обертка контента с SafeAreaView
- Стандартные отступы

`shared/config/index.ts`  Конфигурация приложения  


`env`
```typescript
{
  API_URL: string           // URL API (по умолчанию https://api.fitchoice.app)
  isDevelopment: boolean    // Режим разработки
  isProduction: boolean     // Продакшн режим
}
```

 `constants`
```typescript
{
  APP_NAME: 'Fitchoice',
  APP_SCHEME: 'acme',
  
  POSE_DETECTION: {
    MIN_CONFIDENCE: 0.5,    // Минимальная уверенность позы
    MIN_VISIBILITY: 0.5     // Минимальная видимость точки
  },
  
  STORAGE_KEYS: {
    USER_SETTINGS: '@fitchoice:user_settings',
    WORKOUT_HISTORY: '@fitchoice:workout_history'
  }
}
```


 `shared/lib/utils.ts`  - общие утилиты  


`generateId(): string`
Генерирует уникальный ID (timestamp + random)
`delay(ms): Promise<void>`
Создает задержку (для анимаций, дебаунса)
`clamp(value, min, max): number`
Ограничивает значение в диапазоне
`formatNumber(value, decimals): string`
Форматирует число с округлением


`shared/types/index.ts` глобальные типы  


`ApiResponse<T>`
Стандартный ответ API
```typescript
{
  data: T
  status: number
  message?: string
}
```

 `ApiError`
Ошибка API
```typescript
{
  message: string
  status: number
  code?: string
}
```

 Utility Types
- `Nullable<T>` = T | null
- `Optional<T>` = T | undefined
- `ID` = string | number


 ТЕКУЩЕЕ СОСТОЯНИЕ

Что работает:
-  Приложение запускается
-  Камера с детекцией поз (MediaPipe)
-  Получение 33 точек тела в реальном времени
-  Логирование координат точек
-  Header с навигацией
-  Footer с copyright
-  FSD архитектура настроена
-  TypeScript полностью типизирован
-  ESLint без ошибок

Что готово, но не используется:
- `Button` - базовая кнопка (ждет использования)
- `Container` - контейнер с отступами
- TanStack Query - настроен, ждет API
- Zustand - установлен, ждет stores
- `PoseScreen` - fullscreen камера (роут не настроен)
- Функции анализа поз (`calculateAngle`, `calculateDistance`) - готовы для упражнений

### 📋 Что нужно добавить (будущее):
-  Обработка поз для упражнений (приседания, отжимания)
-  Подсчет повторений
-  Проверка правильности выполнения
-  Сохранение истории тренировок
-  Статистика и аналитика
-  API интеграция
-  Stores для state management

---

ЗАВИСИМОСТИ МЕЖДУ МОДУЛЯМИ

```
app/index.tsx
  └─> screens/home
      └─> widgets/header
      └─> widgets/footer
      └─> widgets/pose-camera
          └─> entities/pose (types, analyzer)
              └─> shared/config
```

 Слои могут импортировать только из нижележащих слоев:
- `screens` → `widgets` → `entities` → `shared`
-  `shared` НЕ может импортировать из `entities`
-  `entities` НЕ может импортировать из `widgets`

установленные пакеты

 Core
- `expo` - фреймворк для React Native
- `react-native` - кросс-платформенная разработка
- `expo-router` - file-based роутинг

 UI & Styling
- `nativewind` - Tailwind CSS для React Native
- `react-native-safe-area-context` - безопасные зоны

 State Management
- `zustand` 5.0.8 - client state (установлен, не используется)
- `@tanstack/react-query` 5.90.3 - server state (настроен, не используется)

 Utilities
- `date-fns` 4.1.0 - работа с датами
- `@react-native-async-storage/async-storage` 2.2.0 - локальное хранилище

 AI & Computer Vision
- `@thinksys/react-native-mediapipe` 0.0.16 - детекция поз

---

TODO

Ближайшие задачи:

1. Экран для входа (Login Screen)
  - Экран Лэндинга 
  - Экран регистрации
  - Экран входа 
  - Экран верификации и переотправки кода
  
- адаптивность 
- интеграция с бэком

2. Экран с прохождением теста (Test Screen)
   - Создать screens/test/
   - Логика прохождения теста
   

3. Остальной функционал
   - Ждем реализации макетов от дизайнера
   - После получения макетов планируем разработку

4. ⚠️ ВРЕМЕННОЕ: Кнопка "Тест: Опрос" на LandingScreen
   - Добавлена для удобства тестирования опроса
   - **ОБЯЗАТЕЛЬНО УДАЛИТЬ ПЕРЕД РЕЛИЗОМ В ПРОД**
   - Файл: `src/screens/landing/ui/LandingScreen.tsx`
   - Кнопка с текстом "⚠️ Тест: Опрос" и фиолетовым фоном

---
